<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="./bootstrap/bootstrap.min.css" />
        <link rel="stylesheet" href="./css/style.css" />
        <link rel="stylesheet" href="./css/index.css" />
        <script src="./script/index.js" defer></script>
        <title>ISLA | HOME</title>
    </head>
    <body class="d-flex justify-content-center align-items-center flex-column min-vh-100 h-auto position-relative">
        <%- include("navbar") %>
        <div class="container root mw-100 vw-100">
            <h2 class="title text-dark">URL Shortener</h2>
            <form action="/api/create" class="form d-flex justify-content-start align-items-center mt-5" method="POST">
                <input type="url" name="url" placeholder="Url" id="input-url" class="form-control me-3 col" />
                <button type="submit" class="btn btn-primary">Create</button>
            </form>
            <table class="table table-striped table-data mt-5 text-black">
                <thead>
                    <tr class="text-center">
                        <th>No</th>
                        <th>URL</th>
                        <th>Short</th>
                        <th>Visit</th>
                    </tr>
                </thead>
                <tbody data-table-body></tbody>
            </table>
        </div>
    </body>
</html>
